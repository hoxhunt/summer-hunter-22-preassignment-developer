const input = require("./data.js");

// Complete this function
const countUsersWithTooManyReusedPasses = (data) => {
  return 0;
};

console.log(countUsersWithTooManyReusedPasses(input));

// Write your solution here:
// ___
// it should be a two digit number with both digits the same (eg. 11)

// Once you are done create a pastebin (https://pastebin.com/) from this file.
// Select the following options:
// - Syntax highlighting: Javascript
// - Paste exposure: Unlisted

// Then reply to the recruiting manager with the pastebin link.
